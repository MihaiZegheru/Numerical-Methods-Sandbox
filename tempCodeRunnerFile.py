	# print(np.linalg.eig(np.transpose(A) * A))
	# print(eigen_values)
	# eigen_values = np.transpose(np.matrix(eigen_values))

	# sq_size = min(n, m)
	# singular_values = np.sqrt(eigen_values)
	# E = np.zeros((sq_size, sq_size))

	# np.fill_diagonal(E, singular_values)
	# # print(E)

	# # print(np.transpose(A) * A)
	# V = np.concatenate(eigen_vectors, axis=1)
	# # print(V)

	# # print(V)
	# # print(eigen_values)
	# # print(singular_values)
	# # print(eigen_vectors[0])